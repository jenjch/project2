// may need multiple API routes files. i.e getting all collections by a certain user, getting collections, etc.

var db = require("../models");

module.exports = function(app) {

// app.get ... look at post/author activity example
};